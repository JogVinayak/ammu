plugins {
	id 'java'
	id 'org.springframework.boot' version '4.0.1'
	id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.learning'
version = '0.0.1-SNAPSHOT'
description = 'Demo project for Spring Boot'

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(25)
	}
}

repositories {
	mavenCentral()
}

dependencies {
	implementation 'org.springframework.boot:spring-boot-starter'
	implementation 'org.springframework.boot:spring-boot-starter-web'
	implementation 'org.springframework.boot:spring-boot-starter-webflux'
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
	implementation 'org.liquibase:liquibase-core'
	compileOnly 'org.projectlombok:lombok'
	annotationProcessor 'org.projectlombok:lombok'
	testImplementation 'org.springframework.boot:spring-boot-starter-test'
	runtimeOnly 'com.h2database:h2'
	runtimeOnly 'org.postgresql:postgresql'
	testCompileOnly 'org.projectlombok:lombok'
	testAnnotationProcessor 'org.projectlombok:lombok'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.named('test') {
	useJUnitPlatform()
}

// Docker configuration
def dockerImageName = 'auth-service'
def dockerImageTag = version
def containerName = 'auth-service'
def containerPort = 8081

tasks.register('dockerBuild', Exec) {
	group = 'docker'
	description = 'Build Docker image for auth-service'
	dependsOn bootJar
	commandLine 'docker', 'build', '-t', "${dockerImageName}:${dockerImageTag}", '-t', "${dockerImageName}:latest", '.'
}

tasks.register('dockerStop', Exec) {
	group = 'docker'
	description = 'Stop and remove auth-service container'
	commandLine 'sh', '-c', "docker stop ${containerName} 2>/dev/null || true && docker rm ${containerName} 2>/dev/null || true"
}

tasks.register('dockerRun', Exec) {
	group = 'docker'
	description = 'Run auth-service container'
	dependsOn dockerBuild, dockerStop
	commandLine 'docker', 'run', '-d',
		'--name', containerName,
		'-p', "${containerPort}:${containerPort}",
		'--network', 'learner-network',
		'-e', 'SPRING_DATASOURCE_URL=jdbc:postgresql://auth-postgres:5432/auth',
		'-e', 'SPRING_DATASOURCE_USERNAME=auth_user',
		'-e', 'SPRING_DATASOURCE_PASSWORD=auth_password',
		"${dockerImageName}:latest"
}

tasks.register('dockerDeploy') {
	group = 'docker'
	description = 'Build and deploy auth-service to Docker'
	dependsOn dockerRun
	doLast {
		println "auth-service deployed successfully!"
		println "Container: ${containerName}"
		println "Port: ${containerPort}"
		println "Health check: http://localhost:${containerPort}/actuator/health"
	}
}

tasks.register('dockerLogs', Exec) {
	group = 'docker'
	description = 'Show auth-service container logs'
	commandLine 'docker', 'logs', '-f', containerName
}

tasks.register('dockerStatus', Exec) {
	group = 'docker'
	description = 'Show auth-service container status'
	commandLine 'docker', 'ps', '-a', '--filter', "name=${containerName}"
}
